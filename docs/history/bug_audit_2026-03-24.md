# Cent Jours Bug 检查与修复计划

> 日期：2026-03-24
> 分支：`auto/gameplay_update`
> 范围：主菜单、地图交互、回合推进、存读档 UX

## 本轮目标

- 先把主菜单和主回合链里的高优先级 bug 清完，再继续功能开发
- 修复顺序以“卡死 / 流程错误”优先，其次是“遮挡 / 排版 / 交互成本”，最后是“可用性增强”
- 全部修完后再统一更新开发文档

## 已确认 bug

| ID | 优先级 | 状态 | 现象 | 已确认根因 / 插点 |
|----|--------|------|------|--------------------|
| B01 | P0 | 已修复 | 前 10 天教程文案在地图副标题和 Decision Tray 同时重复出现 | `[main_menu.gd]` 现在拆开了托盘提示和地图副标题，教程文案不再原样重复 |
| B02 | P0 | 已修复 | 悬停地图节点时，检查器面板挡住地图节点 | `[map_controller.gd]` 现已拆成顶部小型 hover 预览和 click 锁定详情，hover 不再占用地图右侧大面板 |
| B03 | P0 | 已修复 | 点击“增加军费”等行动后 UI 卡在“结算中...” | `[turn_manager.gd]` 的 `submit_action()` 改为返回成功/失败；`[main_menu.gd]` 会在提交失败时恢复交互，读档后阶段也会强制回到 `action` |
| B04 | P0 | 已修复 | 悬停预览和点击后的城市信息排版错乱，出现极窄列逐字换行 | `[main_menu.tscn]` / `[layout_controller.gd]` 已加宽锁定详情面板并给内容设置最小宽度，hover 预览也改成独立布局 |
| B05 | P1 | 已修复 | 地图过大且节点过密，当前不可缩放 | `[main_menu.tscn]` / `[map_controller.gd]` 现在支持 `MapScroll + 滚轮缩放 + 右键复位` |
| B06 | P0 | 已修复 | 读档按钮不能选择存档槽，也没有常驻“重新开始游戏”按钮 | `[save_manager.gd]` 已升级成 3 槽位；主菜单顶栏现有 `新局 / 存档 / 读档` |
| B07 | P0 | 已修复 | “执行行动”和“进入下一回合”的关系不清楚 | `[main_menu.gd]` 的确认按钮和禁用提示已明确写成“执行今日行动 → 次日 / 正在结算并进入次日…” |
| B08 | P0 | 进行中 | 需要全面扫描项目其他潜在 bug，避免只补图里这几处 | 本文档即本轮扫描台账，后续新增问题继续追加 |

## 额外扫描发现

| ID | 优先级 | 状态 | 现象 | 已确认根因 / 插点 |
|----|--------|------|------|--------------------|
| B09 | P0 | 已修复 | 读档成功后的回合阶段和按钮状态可能不一致 | `[turn_manager.gd]::load_from_save()` 现在会把阶段恢复到 `action` 并重新广播 `phase_changed` |
| B10 | P1 | 已修复 | Hover 预览和锁定详情没有分层，导致地图信息阅读成本高 | `[map_controller.gd]::refresh_map_inspector()` 已拆成 hover 小预览和 click 锁定详情两层 |
| B11 | P1 | 已修复 | 常驻顶栏缺少“新游戏 / 重新开始”入口，只能在结局弹窗里重开 | `[main_menu.gd]` 顶栏已补 `新局` 按钮，并带确认弹窗 |
| B12 | P1 | 已修复 | `SituationPanel` 长文本会把侧栏空间挤爆 | 侧栏已改成内部滚动并限制高度，长文本不再撑爆右栏 |
| B13 | P0 | 已修复 | 存档 / 读档弹窗点击时报 `theme_override_constants` 访问错误 | `[main_menu.gd]::_show_slot_picker()` 之前直接写 `VBoxContainer.theme_override_constants`；现已改成 `add_theme_constant_override()` |
| B14 | P0 | 已修复 | 行动后页面被日志面板撑大，内容超出窗口 | `[main_menu.tscn]` / `[layout_controller.gd]` 给 `NarrativePanel` 增加了内部滚动和固定高度上限 |
| B15 | P1 | 已修复 | Hover 预览与锁定详情位置切换太突兀 | 两种面板现在都固定在地图右上角同一块区域内，只是高度不同 |
| B16 | P1 | 已缓解 | 玩家仍会疑惑“一回合到底做几次事” | 当前 UI 已明确成“执行今日行动 -> 次日”；若后续要做“结束回合按钮”，属于规则设计变更，不在本轮直接改模型 |
| B17 | P0 | 已修复 | 打开存档 / 读档槽位时，槽位标签格式化在 `in_progress` 状态下崩溃 | `[save_manager.gd]` 已把槽位 `outcome` 格式化从 `String(...)` 改成 `str(...)`，并由 `GdUnit4` 覆盖多槽存读档可用性 |

## 修复顺序

1. 已完成 `B03` 回合卡死 / 结算链异常护栏
2. 已完成 `B09` 读档阶段错位修复
3. 已完成 `B02` / `B04` 悬停遮挡与地图检查器排版修复
4. 已完成 `B01` / `B07` 教程重复与动作语义收口
5. 已完成 `B06` / `B11` 多槽存读档与常驻重开入口
6. 已完成 `B05` / `B10` 地图缩放与 hover / click 分层体验
7. 已完成 Windows 主项目无头、Windows smoke scene、Windows GUI 启动冒烟
8. 已完成 `GdUnit4` 对多槽存读档、叙事面板滚动链与区域任务显示的自动回归

## 验证要求

- 只做 Windows 路径验证
- 主菜单 / 回合链改动后至少跑：
  - Windows `cargo build --features godot-extension`
  - Windows Godot headless 主项目
  - Windows smoke scene
- 若某项修复无法在当前轮完整验证，必须在文档里明确记“未验证”
